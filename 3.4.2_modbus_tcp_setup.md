# 3.4.2 การตั้งค่า Modbus TCP ในระบบ

หัวข้อนี้อธิบายการตั้งค่า Modbus TCP Server ในระบบ โดยอ้างอิงจากโค้ดจริงในโปรเจกต์เท่านั้น ไฟล์หลักที่เกี่ยวข้องคือ `app.py`, `routes/app_routes.py` และ `services/printer_service.py`

ระบบนี้ใช้ Modbus TCP เพื่อส่งสัญญาณจาก Raspberry Pi / Web Application ไปยังอุปกรณ์ภายนอก เช่น Cobot หรือ TM Flow โดยระบบทำหน้าที่เป็น Modbus TCP Server และเปิดให้ Cobot เข้ามาเชื่อมต่อเพื่ออ่านค่าสัญญาณจาก address ที่กำหนด

## 1. Library ที่ใช้สำหรับ Modbus TCP

ในไฟล์ `app.py` มีการ import library จาก `pymodbus`

```python
from pymodbus.server import StartAsyncTcpServer
from pymodbus.datastore import ModbusSequentialDataBlock, ModbusDeviceContext, ModbusServerContext
```

โค้ดนี้ใช้สำหรับสร้าง Modbus TCP Server และสร้างพื้นที่เก็บข้อมูลของ Modbus เช่น Coils, Discrete Inputs, Holding Registers และ Input Registers

## 2. การสร้าง Modbus Data Store

ในไฟล์ `app.py` มีการสร้าง Modbus data store ดังนี้

```python
# --- Modbus Setup ---
slaves_store = {1: ModbusDeviceContext(di=ModbusSequentialDataBlock(0, [0]*100), co=ModbusSequentialDataBlock(0, [0]*100), hr=ModbusSequentialDataBlock(0, [0]*100), ir=ModbusSequentialDataBlock(0, [0]*100))}
context = ModbusServerContext(devices=slaves_store, single=False)
```

จากโค้ดนี้ ระบบสร้าง Modbus Device ที่ slave id `1` และสร้างตารางข้อมูล 4 ประเภท ได้แก่

- `di` คือ Discrete Inputs
- `co` คือ Coils
- `hr` คือ Holding Registers
- `ir` คือ Input Registers

แต่ละตารางเริ่มที่ address `0` และมีจำนวน `100` ตำแหน่ง โดยค่าเริ่มต้นทั้งหมดเป็น `0`

## 3. การเขียนค่าสัญญาณลง Address

ฟังก์ชัน `_set_modbus_signal()` ในไฟล์ `app.py` ใช้สำหรับเขียนค่า `0` หรือ `1` ลง Modbus address

```python
def _set_modbus_signal(addr, value):
    addr = int(addr)
    bit_value = 1 if value else 0
    # Mirror the same address across common Modbus tables so TM Flow can read the signal
    # even if its node is configured for a different table type.
    context[1].setValues(0x01, addr, [bit_value])
    context[1].setValues(0x02, addr, [bit_value])
    context[1].setValues(0x03, addr, [bit_value])
    context[1].setValues(0x04, addr, [bit_value])
```

โค้ดนี้รับ `addr` และ `value` จากนั้นแปลง `value` เป็น `bit_value`

- ถ้า `value=True` จะเขียนค่า `1`
- ถ้า `value=False` จะเขียนค่า `0`

ระบบเขียนค่าเดียวกันลงทั้ง 4 ตารางของ Modbus ด้วย function code:

- `0x01`
- `0x02`
- `0x03`
- `0x04`

ใน comment ของโค้ดระบุว่า ระบบ mirror ค่า address เดียวกันไปยังหลายตาราง เพื่อให้ TM Flow อ่านสัญญาณได้ แม้ node ใน TM Flow จะถูกตั้งค่าเป็น table type ที่ต่างกัน

## 4. การส่งสัญญาณแบบ Pulse

ฟังก์ชัน `trigger_modbus_signal()` ในไฟล์ `app.py` ใช้ส่งสัญญาณ Modbus แบบ pulse

```python
def trigger_modbus_signal(addr):
    try:
        _set_modbus_signal(addr, True)
        def reset():
            time.sleep(2)
            _set_modbus_signal(addr, False)
        threading.Thread(target=reset, daemon=True).start()
        _append_modbus_log(addr, "SENT")
    except Exception:
        _append_modbus_log(addr, "ERROR")
```

ขั้นตอนการทำงานคือ

1. เขียนค่า `1` ไปยัง address ที่กำหนด
2. สร้าง thread สำหรับ reset ค่า
3. รอ `2` วินาที
4. เขียนค่ากลับเป็น `0`
5. บันทึก log ว่าส่งสำเร็จหรือ error

ดังนั้นสัญญาณที่ Cobot อ่านได้จะเป็นสัญญาณชั่วคราว ไม่ได้ค้างเป็น `1` ตลอดเวลา

## 5. การเก็บ Log การส่ง Modbus

ระบบมีฟังก์ชัน `_append_modbus_log()` สำหรับบันทึกประวัติการส่งสัญญาณ

```python
def _append_modbus_log(addr, status):
    entry = {
        "timestamp": datetime.datetime.now().strftime("%H:%M:%S"),
        "addr": int(addr),
        "status": str(status),
    }
    with data_lock:
        modbus_log.append(entry)
        if len(modbus_log) > 30:
            modbus_log.pop(0)
```

Log ที่บันทึกประกอบด้วย

- เวลาในการส่ง
- address ที่ส่ง
- สถานะ เช่น `SENT` หรือ `ERROR`

ระบบเก็บ log ล่าสุดไว้ไม่เกิน 30 รายการ

## 6. การเปิด Modbus TCP Server

ในไฟล์ `app.py` มีฟังก์ชัน `run_modbus_server()` และ `start_modbus_thread()`

```python
async def run_modbus_server():
    await StartAsyncTcpServer(context=context, address=("0.0.0.0", 5020))

def start_modbus_thread():
    loop = asyncio.new_event_loop(); asyncio.set_event_loop(loop); loop.run_until_complete(run_modbus_server())
```

โค้ดนี้เปิด Modbus TCP Server ที่ address:

```text
0.0.0.0:5020
```

ความหมายคือ Server จะ listen ทุก network interface ของเครื่อง และใช้ port `5020` สำหรับ Modbus TCP

## 7. การเริ่ม Modbus Server ตอนระบบเริ่มทำงาน

ในส่วนท้ายของไฟล์ `app.py` เมื่อระบบเริ่มทำงาน จะสร้าง thread สำหรับ Modbus TCP Server

```python
threading.Thread(target=start_modbus_thread, daemon=True).start()
```

การรันเป็น `daemon=True` ทำให้ Modbus TCP Server ทำงานอยู่เบื้องหลังพร้อมกับระบบหลัก เช่น Flask Web Application, AI inference, MQTT printer และ system monitor

## 8. การเชื่อมต่อเครือข่ายกับ Cobot

จากโค้ดจริง ระบบเปิด Modbus TCP Server ที่

```python
await StartAsyncTcpServer(context=context, address=("0.0.0.0", 5020))
```

ดังนั้นฝั่ง Cobot หรือ TM Flow ต้องเชื่อมต่อมายัง IP ของ Raspberry Pi หรือเครื่องที่รันระบบนี้ และใช้ port:

```text
5020
```

ระบบมีฟังก์ชัน `get_all_ips()` เพื่อดึง IP ของเครื่อง

```python
def get_all_ips():
    now_ts = time.time()
    if now_ts - _ip_cache["ts"] < 10.0:
        return _ip_cache["value"]

    ips = []
    try:
        ips = subprocess.check_output(["hostname", "-I"]).decode().split()
        ips = [ip for ip in ips if "." in ip]
    except Exception:
        ips = []
    _ip_cache["value"] = ips
    _ip_cache["ts"] = now_ts
    return ips
```

และใน API `/get_status` ระบบส่ง `server_ip` และ `modbus_port` กลับไปยังหน้าเว็บ

```python
"server_ip": all_ips[0] if len(all_ips) > 0 else "127.0.0.1",
"backup_ip": all_ips[1] if len(all_ips) > 1 else None,
"modbus_port": 5020,
"robot_status": runtime.get_robot_status(),
```

จากโค้ดนี้ หน้า Web Application สามารถแสดง IP และ port ที่ Cobot ต้องใช้สำหรับเชื่อมต่อ Modbus TCP ได้

## 9. การตรวจสอบสถานะการเชื่อมต่อ Cobot

ระบบตรวจสอบว่า Cobot เชื่อมต่อเข้ามาที่ port `5020` แล้วหรือไม่ด้วยฟังก์ชัน `get_robot_status()` ในไฟล์ `app.py`

```python
def get_robot_status():
    now_ts = time.time()
    if now_ts - _robot_status_cache["ts"] < 1.0:
        return _robot_status_cache["value"]

    status = "Disconnected"
    try:
        for conn in psutil.net_connections(kind="tcp"):
            if conn.laddr and conn.laddr.port == 5020 and conn.status == "ESTABLISHED":
                status = "Connected"
                break
    except Exception:
        pass
    _robot_status_cache["value"] = status
    _robot_status_cache["ts"] = now_ts
    return status
```

ฟังก์ชันนี้ตรวจ TCP connection ของเครื่อง ถ้ามี connection ที่ local port `5020` และสถานะเป็น `ESTABLISHED` ระบบจะถือว่า Cobot เชื่อมต่ออยู่ และแสดงสถานะเป็น `Connected`

## 10. API สำหรับ Trigger Modbus Address จากหน้าเว็บ

ในไฟล์ `routes/app_routes.py` มี API สำหรับ trigger address ที่ต้องการ

```python
@app.route("/api/modbus/trigger", methods=["POST"])
def api_modbus_trigger():
    payload = request.get_json(silent=True) or {}
    addr = int(payload.get("addr", payload.get("address", 0)))
    if addr < 0:
        return jsonify({"success": False, "message": "Invalid address."}), 400
    runtime.trigger_modbus_signal(addr)
    return jsonify({"success": True, "address": addr})
```

API นี้รับค่า `addr` หรือ `address` จาก JSON แล้วเรียก `runtime.trigger_modbus_signal(addr)` เพื่อส่งสัญญาณไปยัง address ที่กำหนด

หากไม่ได้ส่งค่า address มา ระบบจะใช้ค่า default เป็น `0`

```python
addr = int(payload.get("addr", payload.get("address", 0)))
```

## 11. Address ที่ใช้ในระบบจากโค้ดจริง

จากการค้นในโค้ดจริง พบ address ที่ถูกใช้งานโดยตรงดังนี้

### Address 0

Address `0` ถูก trigger ได้ 2 จุดจากโค้ดจริง

จุดแรกคือเมื่อผู้ใช้กดให้ระบบพร้อมทำงาน หรือ `SYSTEM READY / NEXT PART` ในไฟล์ `templates/index.html`

```javascript
async function setSystemReady(shouldTriggerModbus = true) {
    if (shouldTriggerModbus) {
        try {
            const res = await fetch('/api/inspection/reset', { method: 'POST' });
            const data = await res.json();
            if (!res.ok || !data.success) throw new Error(data.message || "Unable to reset inspection session");
        } catch (e) {
            showToast(e.message || "Failed to prepare inspection session", "danger");
            return;
        }
    }
            
    if (shouldTriggerModbus) {
        showToast("Handshake OK: Ready for Inspection", "success");
        // Trigger Modbus Addr 0 (System Ready)
        triggerModbus(0);
    }
            
    updateInspectionUI();
}
```

จาก comment ในโค้ด `Trigger Modbus Addr 0 (System Ready)` จึงสรุปได้ว่า address `0` ใช้เป็นสัญญาณ `System Ready`

จุดที่สองคือเมื่อเครื่องพิมพ์เข้าสู่สถานะจบงาน เช่น `FINISH`, `COMPLETE`, `COMPLETED`

โค้ดอยู่ในไฟล์ `services/printer_service.py`

```python
def update_printer_finish_cleanup_timer(runtime, status_text):
    status_upper = str(status_text or "").strip().upper()
    finish_states = {"FINISH", "COMPLETE", "COMPLETED"}
    active_states = {"RUNNING", "PRINTING", "PREPARE", "PREPARING", "PAUSE", "PAUSED"}

    if status_upper in finish_states:
        if runtime._printer_finish_cleared:
            return
        if not runtime._printer_post_finish_armed:
            runtime._printer_post_finish_armed = True
            runtime._printer_post_finish_deadline_ts = time.time() + 30.0
        if not runtime._printer_finish_signal_sent:
            runtime._printer_finish_signal_sent = True
            runtime.trigger_modbus_signal(0)
            should_auto_ready = False
            with runtime.data_lock:
                should_auto_ready = (
                    bool(runtime.system_on)
                    and not bool(runtime.inspection_active)
                )
            if should_auto_ready:
                runtime.start_inspection_session()
        return
```

จากโค้ดนี้ address `0` ใช้เป็นสัญญาณเมื่อเครื่องพิมพ์ 3D พิมพ์เสร็จ และระบบอาจเริ่ม inspection session ต่อหากระบบเปิดอยู่และยังไม่มี inspection ทำงานอยู่

### Address 1

Address `1` ถูก trigger หลังจากระบบบันทึกผล `SIDE 1` สำเร็จ โดยโค้ดอยู่ในไฟล์ `templates/index.html`

```javascript
if (sideToSave < 3) {
    triggerModbus(sideToSave);
}
```

จากโค้ดนี้ ถ้า `sideToSave` มีค่าเป็น `1` ระบบจะเรียก

```javascript
triggerModbus(1);
```

ดังนั้น address `1` ใช้เป็นสัญญาณหลังจากตรวจและบันทึกผล `SIDE 1` แล้ว

### Address 2

Address `2` ถูก trigger หลังจากระบบบันทึกผล `SIDE 2` สำเร็จ โดยใช้โค้ดเดียวกันในไฟล์ `templates/index.html`

```javascript
if (sideToSave < 3) {
    triggerModbus(sideToSave);
}
```

จากโค้ดนี้ ถ้า `sideToSave` มีค่าเป็น `2` ระบบจะเรียก

```javascript
triggerModbus(2);
```

ดังนั้น address `2` ใช้เป็นสัญญาณหลังจากตรวจและบันทึกผล `SIDE 2` แล้ว

ฟังก์ชัน `triggerModbus(addr)` ในไฟล์ `templates/index.html` ส่งค่า address ไปยัง Flask API `/api/modbus/trigger`

```javascript
function triggerModbus(addr) {
    fetch('/api/modbus/trigger', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({addr: addr})
    }).catch(e => console.error("Modbus error", e));
}
```

### Address 3

Address `3` ถูก trigger เมื่อผลตรวจรวมเป็น `GOOD`

โค้ดอยู่ในไฟล์ `app.py`

```python
trigger_modbus_signal(4 if final_result == "NG" else 3)
```

จากโค้ดนี้ ถ้า `final_result` ไม่ใช่ `NG` ระบบจะส่งสัญญาณไปที่ address `3`

### Address 4

Address `4` ถูก trigger เมื่อผลตรวจรวมเป็น `NG`

โค้ดอยู่ในไฟล์ `app.py`

```python
trigger_modbus_signal(4 if final_result == "NG" else 3)
```

จากโค้ดเดียวกัน ถ้า `final_result == "NG"` ระบบจะส่งสัญญาณไปที่ address `4`

## 12. ตารางสรุป Address จากโค้ดจริง

| Address | ความหมายจากโค้ดจริง | แหล่งโค้ด |
|---:|---|---|
| 0 | `System Ready` จากหน้าเว็บ และถูก trigger อีกกรณีเมื่อเครื่องพิมพ์อยู่ในสถานะจบงาน `FINISH`, `COMPLETE`, `COMPLETED` | `templates/index.html`, `services/printer_service.py` |
| 1 | บันทึกผล `SIDE 1` สำเร็จ | `templates/index.html` |
| 2 | บันทึกผล `SIDE 2` สำเร็จ | `templates/index.html` |
| 3 | ผลตรวจรวมเป็น `GOOD` | `app.py` |
| 4 | ผลตรวจรวมเป็น `NG` | `app.py` |
| ค่าอื่น ๆ | สามารถ trigger ผ่าน API `/api/modbus/trigger` ได้ แต่ในโค้ดที่ค้นพบไม่มีความหมายเฉพาะที่ hard-code ไว้ | `routes/app_routes.py` |

## 13. ลำดับการทำงานของ Modbus TCP ในระบบ

ลำดับการตั้งค่าและทำงานของ Modbus TCP จากโค้ดจริงคือ

```text
import pymodbus
→ สร้าง Modbus Data Store สำหรับ slave id 1
→ สร้าง ModbusServerContext
→ เปิด StartAsyncTcpServer ที่ 0.0.0.0 port 5020
→ เริ่ม server ใน background thread
→ Cobot เชื่อมต่อเข้ามาที่ IP ของ Raspberry Pi port 5020
→ ระบบตรวจ TCP connection ที่ port 5020 เพื่อแสดง robot_status
→ เมื่อมีเหตุการณ์สำคัญ ระบบเรียก trigger_modbus_signal(address)
→ เขียนค่า 1 ไปยัง address นั้น
→ รอ 2 วินาที
→ reset ค่า address กลับเป็น 0
```

## 14. สรุปการตั้งค่าฝั่งระบบและ Cobot

จากโค้ดจริง ฝั่งระบบตั้งค่า Modbus TCP ดังนี้

- เครื่องที่รันระบบทำหน้าที่เป็น Modbus TCP Server
- เปิดรับ connection ที่ `0.0.0.0`
- ใช้ port `5020`
- ใช้ slave id `1`
- สร้าง address 0-99 ในตาราง `di`, `co`, `hr`, `ir`
- เขียน signal ลงทุก table type เพื่อให้ TM Flow อ่านได้แม้ตั้งค่า node ต่างกัน
- ส่งสัญญาณแบบ pulse 2 วินาที

ฝั่ง Cobot หรือ TM Flow ต้องเชื่อมต่อมายัง

```text
IP Address: server_ip ของ Raspberry Pi / เครื่องที่รันระบบ
Port: 5020
Slave ID: 1
Address: ตามสัญญาณที่ต้องการอ่าน เช่น 0, 3, 4
```

โดยค่า `server_ip` และ `modbus_port` ถูกส่งไปแสดงบนหน้าเว็บผ่าน API `/get_status`

```python
"server_ip": all_ips[0] if len(all_ips) > 0 else "127.0.0.1",
"modbus_port": 5020,
```

ดังนั้นการเชื่อมต่อเครือข่ายกับ Cobot คือให้ Cobot อยู่ในเครือข่ายเดียวกับ Raspberry Pi แล้วตั้งค่าให้ Cobot อ่าน Modbus TCP จาก IP ของ Raspberry Pi ที่ port `5020`
