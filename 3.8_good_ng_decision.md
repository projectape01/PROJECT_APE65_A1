# 3.8 การตัดสินของดีหรือของเสีย

หัวข้อนี้อธิบายการตัดสินผลของระบบจากโค้ดจริงในโปรเจกต์ โดยระบบแบ่งการตรวจสอบออกเป็น 3 ด้าน คือ `SIDE 1`, `SIDE 2` และ `SIDE 3` จากนั้นนำผลของทุกด้านมารวมกันเพื่อสรุปว่าชิ้นงานเป็น `GOOD` หรือ `NG`

## 3.8.1 ภาพรวมการตัดสินผล

ในไฟล์ `app.py` มีการกำหนดด้านที่ต้องตรวจสอบไว้ทั้งหมด 3 ด้าน ดังนี้

```python
ACTIVE_INSPECTION_SIDES = (1, 2, 3)
```

หมายความว่าชิ้นงาน 1 ชิ้นต้องผ่านการตรวจสอบครบทั้ง `SIDE 1`, `SIDE 2` และ `SIDE 3` ก่อนที่ระบบจะสรุปผลสุดท้าย

หลักการตัดสินผลรวมอยู่ในฟังก์ชัน `compute_final_result()` ของไฟล์ `app.py`

```python
def compute_final_result(session):
    has_ng = any(str(session.get(f"side{i}", "")).startswith("NG_") for i in ACTIVE_INSPECTION_SIDES)
    return "NG" if has_ng else "GOOD"
```

จากโค้ดนี้ ระบบจะตรวจสอบค่าผลลัพธ์ของแต่ละด้านใน `part_session` เช่น `side1`, `side2` และ `side3` หากพบว่าด้านใดด้านหนึ่งมีค่าขึ้นต้นด้วย `NG_` ระบบจะสรุปผลรวมของชิ้นงานเป็น `NG` ทันที แต่ถ้าทุกด้านไม่พบ `NG_` ระบบจะสรุปผลเป็น `GOOD`

## 3.8.2 การตัดสิน SIDE 1 และ SIDE 2 ด้วยโมเดล AI

การตรวจสอบ `SIDE 1` และ `SIDE 2` ใช้โมเดล YOLO ตรวจจับข้อบกพร่องจากภาพ โดยโค้ดหลักอยู่ในไฟล์ `app.py` ภายในฟังก์ชัน `ai_inference_thread()` และใช้ฟังก์ชันช่วยจากไฟล์ `utils/inspection_utils.py`

### การแยก Label ว่าเป็น GOOD หรือ NG

ไฟล์ `utils/inspection_utils.py` มีฟังก์ชันสำหรับตรวจสอบว่า label ที่โมเดลตรวจพบเป็นของดีหรือของเสีย

```python
def is_good_label(label):
    text = str(label).strip().upper()
    return text in {"GOOD", "OK", "PASS"} or text.startswith("GOOD")


def is_ng_label(label):
    text = str(label).strip().upper()
    return any(token in text for token in ["NG", "DEFECT", "FAIL", "BAD", "SCRATCH", "BURR"])
```

จากโค้ดนี้ ถ้า label เป็นคำว่า `GOOD`, `OK`, `PASS` หรือขึ้นต้นด้วย `GOOD` ระบบจะถือว่าเป็นของดี แต่ถ้า label มีคำที่สื่อถึงของเสีย เช่น `NG`, `DEFECT`, `FAIL`, `BAD`, `SCRATCH` หรือ `BURR` ระบบจะถือว่าเป็นข้อบกพร่อง

### การประมวลผลภาพด้วย YOLO

ในไฟล์ `app.py` ระบบนำภาพจากกล้องเข้าโมเดล YOLO แล้วอ่านผลลัพธ์ที่โมเดลตรวจพบ

```python
results = model(img, imgsz=YOLO_INPUT_SIZE, conf=YOLO_BASE_CONF, verbose=False)

if results and len(results) > 0:
    res_list = results[0].obb if (hasattr(results[0], 'obb') and results[0].obb is not None) else results[0].boxes
    if res_list is not None and len(res_list) > 0:
        all_detections = []
        for r in res_list:
            lbl = results[0].names[int(r.cls[0])]
            prob = float(r.conf[0])
            if prob < class_conf_threshold(lbl, CLASS_CONF_THRESHOLDS):
                continue
```

โค้ดส่วนนี้ทำหน้าที่ส่งภาพเข้าโมเดล YOLO แล้วดึงค่า `label` และ `confidence` หรือความมั่นใจของผลตรวจจับออกมา หากค่าความมั่นใจต่ำกว่า threshold ที่กำหนด ระบบจะข้ามผลตรวจจับนั้นไป

หลังจากนั้นระบบจะเก็บข้อมูลผลตรวจจับที่ผ่านเงื่อนไขไว้ใน `all_detections`

```python
if cv2.contourArea(np.array(pts).astype(np.int32)) > 500:
    all_detections.append({
        "label": lbl,
        "prob": prob,
        "rank_score": class_rank_score(lbl, prob, CLASS_SCORE_WEIGHTS),
        "pts": pts,
        "is_good": is_good_label(lbl),
        "is_ng": is_ng_label(lbl),
        "side": get_label_side(lbl),
    })
```

ในข้อมูลที่บันทึกไว้ ระบบจะเก็บทั้งชื่อ label, ค่าความมั่นใจ, ตำแหน่งกรอบตรวจจับ, ผลว่าเป็น `GOOD` หรือ `NG` และด้านของชิ้นงานที่ label นั้นเกี่ยวข้อง

### การเลือกผลตรวจจับที่ตรงกับ Side ปัจจุบัน

เมื่อระบบกำลังตรวจด้านใดด้านหนึ่ง จะกรองผลตรวจจับให้ตรงกับด้านปัจจุบัน เช่น ตรวจ `SIDE 1` ก็จะเลือกผลที่เกี่ยวข้องกับ `SIDE 1`

```python
if all_detections:
    relevant_detections = all_detections
    if active_inspection:
        relevant_detections = [
            d for d in all_detections
            if d["side"] in (0, current_side)
        ]

    ng_only = [d for d in relevant_detections if d["is_ng"] and d["pts"]]
    good_only = [d for d in relevant_detections if d["is_good"]]
```

จากโค้ดนี้ ระบบแบ่งผลตรวจจับออกเป็น 2 กลุ่ม คือ `ng_only` สำหรับผลที่เป็นของเสีย และ `good_only` สำหรับผลที่เป็นของดี

### การตัดสินผลของ SIDE 1 และ SIDE 2

ถ้าระบบพบผลตรวจจับที่เป็น `NG` จะเลือกข้อบกพร่องที่มีความสำคัญที่สุด แล้วกำหนดสถานะว่าเป็นของเสีย

```python
if ng_only and not block_overlay:
    target = choose_priority_ng(ng_only, SCRATCH_OVERRIDE_MARGIN)
    best_label = target["label"]
    best_prob = target["prob"]
    best_pts = target["pts"]
    master_ng_found = True

    locked_overlay = {
        "label": best_label,
        "prob": best_prob,
        "points": best_pts,
        "detections": overlay_detections,
        "is_ng": True,
        "side": current_side,
    }
```

แต่ถ้าไม่พบ `NG` และพบผลที่เป็น `GOOD` ระบบจะกำหนดให้ผลของด้านนั้นเป็น `GOOD`

```python
elif good_only:
    target = max(good_only, key=lambda d: d["rank_score"])
    best_label = "GOOD"
    best_prob = target["prob"]
    best_pts = []
    master_ng_found = False
```

เมื่อต้องบันทึกผลของ `SIDE 1` หรือ `SIDE 2` ระบบเรียกใช้ฟังก์ชัน `resolve_side_ai_decision_unlocked(side)` ในไฟล์ `app.py`

```python
def resolve_side_ai_decision_unlocked(side):
    overlay = get_side_ai_overlay_unlocked(side)
    overlay_label = str(overlay.get("label") or "").strip()
    overlay_side = int(get_label_side(overlay_label) or 0)
    overlay_is_ng = bool(overlay.get("is_ng")) and overlay_side in (0, int(side))

    if overlay_is_ng and is_ng_label(overlay_label):
        return {
            "saved_label": f"NG_{int(side)}",
            "defect_label": overlay_label,
            "overlay": overlay,
        }

    overlay["points"] = []
    overlay["detections"] = []
    overlay["is_ng"] = False
    overlay["label"] = "GOOD"
    return {
        "saved_label": f"GOOD_{int(side)}",
        "defect_label": "-",
        "overlay": overlay,
    }
```

ฟังก์ชันนี้เป็นจุดที่กำหนดผลบันทึกของ `SIDE 1` และ `SIDE 2` ถ้าผล AI เป็น `NG` ระบบจะบันทึกเป็น `NG_1` หรือ `NG_2` พร้อมชื่อ defect ที่ตรวจพบ แต่ถ้าไม่เข้าเงื่อนไข `NG` ระบบจะบันทึกเป็น `GOOD_1` หรือ `GOOD_2`

## 3.8.3 การตัดสิน SIDE 3 ด้วย OpenCV

สำหรับ `SIDE 3` ระบบไม่ได้ใช้ YOLO เป็นตัวหลักในการตัดสิน แต่ใช้ OpenCV วัดขนาดของชิ้นงาน ได้แก่ ความกว้างด้านบน ความกว้างด้านล่าง และความยาว จากนั้นนำค่าที่วัดได้ไปเปรียบเทียบกับค่ามาตรฐาน

ค่ามาตรฐานของ `SIDE 3` ถูกกำหนดไว้ในไฟล์ `app.py`

```python
SIDE3_TARGETS_MM = {
    "top": 19.50,
    "bottom": 24.50,
    "length": 90.00,
}
SIDE3_TOLERANCE_MM = 0.3
```

จากโค้ดนี้ ระบบกำหนดค่ามาตรฐานไว้ 3 ค่า คือ

- ความกว้างด้านบน `19.50 mm`
- ความกว้างด้านล่าง `24.50 mm`
- ความยาว `90.00 mm`

และกำหนดค่าความคลาดเคลื่อนที่ยอมรับได้เป็น `±0.3 mm`

### การวัดขนาดด้วย OpenCV

โค้ดวัดขนาดของ `SIDE 3` อยู่ในไฟล์ `utils/side3_measurement.py` ฟังก์ชัน `measure_side3_from_frame(frame, scale_mm_per_pixel)`

```python
def measure_side3_from_frame(frame, scale_mm_per_pixel):
    _, binary_mask, contour = create_binary_mask(frame)
    contour_mask = build_contour_mask(frame.shape, contour)
    bbox = cv2.boundingRect(contour)
    x, y, w, h = bbox
    contour_area = float(cv2.contourArea(contour))
    frame_h, frame_w = frame.shape[:2]
    rect = cv2.minAreaRect(contour)
    ordered_box = _order_box_points(cv2.boxPoints(rect))
```

โค้ดส่วนนี้ใช้ OpenCV สร้าง binary mask, หา contour ของชิ้นงาน และคำนวณกรอบของวัตถุจากภาพ

จากนั้นระบบคำนวณแกนหลักของชิ้นงานและวัดระยะตามแกนดังกล่าว

```python
contour_points = contour.reshape(-1, 2).astype(np.float32)
relative_points = contour_points - center
projections = relative_points @ axis_unit
proj_min = float(np.min(projections))
proj_max = float(np.max(projections))
major_axis_px = float(proj_max - proj_min)
```

ค่า `major_axis_px` คือความยาวของชิ้นงานในหน่วย pixel ตามแกนหลักของวัตถุ

ระบบวัดความกว้างบริเวณด้านบนและด้านล่างของชิ้นงาน โดยใช้ตำแหน่งบน contour mask

```python
top_width = _width_from_mask_projections(
    contour_mask,
    axis_unit,
    perp_unit,
    center,
    top_target_proj,
    band_half_width=sample_band,
)

bottom_width = _width_from_mask_projections(
    contour_mask,
    axis_unit,
    perp_unit,
    center,
    bottom_target_proj,
    band_half_width=sample_band,
)
```

หลังจากได้ค่าหน่วย pixel แล้ว ระบบแปลงเป็นมิลลิเมตรด้วย `scale_mm_per_pixel`

```python
return {
    "length_px": major_axis_px,
    "length_mm": float(major_axis_px * scale_mm_per_pixel),
    "top_width_px": float(top_width["width_px"]),
    "top_width_mm": float(top_width["width_px"] * scale_mm_per_pixel),
    "bottom_width_px": float(bottom_width["width_px"]),
    "bottom_width_mm": float(bottom_width["width_px"] * scale_mm_per_pixel),
    "scale_mm_per_pixel": float(scale_mm_per_pixel),
}
```

จากโค้ดนี้ ระบบได้ค่าที่ใช้ตัดสิน `SIDE 3` คือ `length_mm`, `top_width_mm` และ `bottom_width_mm`

### การเปรียบเทียบกับค่ามาตรฐาน

ในไฟล์ `app.py` มีฟังก์ชัน `dimensions_in_spec(measurement)` สำหรับตรวจสอบว่าค่าที่วัดได้อยู่ในเกณฑ์หรือไม่

```python
def dimensions_in_spec(measurement):
    if not isinstance(measurement, dict):
        return False
    return (
        abs(float(measurement.get("top_width_mm") or 0.0) - SIDE3_TARGETS_MM["top"]) <= SIDE3_TOLERANCE_MM
        and abs(float(measurement.get("bottom_width_mm") or 0.0) - SIDE3_TARGETS_MM["bottom"]) <= SIDE3_TOLERANCE_MM
        and abs(float(measurement.get("length_mm") or 0.0) - SIDE3_TARGETS_MM["length"]) <= SIDE3_TOLERANCE_MM
    )
```

ฟังก์ชันนี้เปรียบเทียบค่าที่วัดได้กับค่ามาตรฐานทีละค่า ถ้าทั้ง 3 ค่าอยู่ในช่วง tolerance จึงถือว่า `SIDE 3` ผ่านเกณฑ์ หากค่าใดค่าหนึ่งเกิน tolerance จะถือว่าไม่ผ่าน

ในฟังก์ชัน `finalize_current_side()` ของไฟล์ `app.py` ระบบนำผลการวัดมาตัดสินเป็น `GOOD_3` หรือ `NG_3`

```python
measurement = measure_side3_from_frame(
    frame_for_measurement,
    measure_settings["scale_mm_per_pixel"],
)
measurement = smooth_side3_measurement(measurement)
measurement = apply_side3_length_offset(measurement)
in_spec = dimensions_in_spec(measurement)
saved_label = "GOOD_3" if in_spec else "NG_3"
defect_label = "-" if in_spec else "DIMENSION NG"
```

ถ้า `in_spec` เป็นจริง ระบบจะบันทึก `SIDE 3` เป็น `GOOD_3` แต่ถ้าไม่ผ่านเกณฑ์ ระบบจะบันทึกเป็น `NG_3` และกำหนด defect เป็น `DIMENSION NG`

ระบบยังบันทึกค่าขนาดที่วัดได้ลงใน `part_session`

```python
part_session[side_key] = saved_label
part_session[defect_key] = defect_label
part_session["dimension_top"] = measurement_payload["top"]
part_session["dimension_bottom"] = measurement_payload["bottom"]
part_session["dimension_length"] = measurement_payload["length"]
part_session["dimension_status"] = measurement_payload["status"]
part_session["dimension_message"] = measurement_message
```

ดังนั้น `SIDE 3` ใช้ผลการวัดจริงจาก OpenCV เป็นตัวตัดสิน ไม่ใช่ label จาก YOLO เป็นหลัก

## 3.8.4 การรวมผลหลังตรวจครบทุกด้าน

หลังจากตรวจครบทุกด้าน ระบบเรียกใช้ฟังก์ชัน `complete_part_if_ready()` ในไฟล์ `app.py`

```python
def complete_part_if_ready():
    global part_session, locked_overlay, latest_ai_results, inspection_active, overlay_block_until
    global side3_measurement_armed, side3_manual_preview_enabled
    final_session = None
    final_result = None

    with data_lock:
        sides_complete = all(part_session.get(f"side{i}") for i in ACTIVE_INSPECTION_SIDES)
        if not sides_complete:
            return None

        final_session = part_session.copy()
        final_result = compute_final_result(final_session)
```

โค้ดนี้ตรวจสอบก่อนว่ามีผลครบทุกด้านหรือไม่ ถ้ายังไม่ครบจะยังไม่สรุปผล แต่ถ้าครบแล้วจะคัดลอกข้อมูลของชิ้นงานและเรียก `compute_final_result()` เพื่อคำนวณผลสุดท้าย

หลังจากได้ผลสุดท้ายแล้ว ระบบส่งสัญญาณ Modbus และบันทึกผลลงฐานข้อมูล

```python
trigger_modbus_signal(4 if final_result == "NG" else 3)
log_part_to_supabase(final_session)
```

จากโค้ดนี้ หากผลสุดท้ายเป็น `NG` ระบบจะส่งสัญญาณ Modbus address `4` แต่ถ้าเป็น `GOOD` จะส่งสัญญาณ Modbus address `3` แล้วบันทึกข้อมูลการตรวจสอบลง Supabase

## 3.8.5 สรุปการทำงานของระบบ

ระบบตัดสินผลของชิ้นงานจาก 3 ส่วนหลักดังนี้

1. `SIDE 1` และ `SIDE 2` ใช้ YOLO ตรวจจับข้อบกพร่องจากภาพ หากพบ label ที่จัดเป็น `NG` ระบบจะบันทึกเป็น `NG_1` หรือ `NG_2` แต่ถ้าไม่พบ `NG` จะบันทึกเป็น `GOOD_1` หรือ `GOOD_2`
2. `SIDE 3` ใช้ OpenCV วัดขนาดของชิ้นงาน ได้แก่ ความกว้างด้านบน ความกว้างด้านล่าง และความยาว จากนั้นเปรียบเทียบกับค่ามาตรฐานและ tolerance ที่กำหนดไว้ ถ้าผ่านทุกค่าจะเป็น `GOOD_3` แต่ถ้ามีค่าใดไม่ผ่านจะเป็น `NG_3`
3. เมื่อตรวจครบทุกด้าน ระบบรวมผลด้วยฟังก์ชัน `compute_final_result()` หากมีด้านใดด้านหนึ่งขึ้นต้นด้วย `NG_` ผลรวมจะเป็น `NG` แต่ถ้าทุกด้านผ่าน ระบบจะสรุปผลเป็น `GOOD`

กล่าวโดยสรุป ระบบใช้หลักการตัดสินแบบเข้มงวด คือ ชิ้นงานจะถูกตัดสินเป็น `GOOD` ได้ก็ต่อเมื่อ `SIDE 1`, `SIDE 2` และ `SIDE 3` ผ่านทั้งหมด หากมีเพียงด้านใดด้านหนึ่งไม่ผ่าน ระบบจะตัดสินผลรวมเป็น `NG`

## ไฟล์โค้ดที่เกี่ยวข้อง

- `app.py` ใช้สำหรับ YOLO inference, การบันทึกผลแต่ละ side, การตัดสิน SIDE 3 และการรวมผลสุดท้าย
- `utils/inspection_utils.py` ใช้สำหรับแยก label ว่าเป็น `GOOD` หรือ `NG`
- `utils/side3_measurement.py` ใช้สำหรับวัดขนาด SIDE 3 ด้วย OpenCV
